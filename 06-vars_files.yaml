- name: varibles from files
  hosts: local
  connection: local
  vars_files:
  - course.yaml
  tasks:
  - name: print course info
    ansible.builtin.debug:
     msg: "course name is: {{ COURSE }}, Trainer is {{ Trainer}}"
     
